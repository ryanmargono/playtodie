import React from 'react'
import { connect } from 'react-redux'
import { Button } from 'react-materialize';

const Randomizer = props => {
    return (
        <Button> Find Random Resturant </Button>
    )
}